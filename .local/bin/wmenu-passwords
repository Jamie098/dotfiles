#!/bin/bash
login=$(rbw ls | wmenu -l 10 -p "passwords")
if [ -n "$login" ]; then
  user=$(rbw get git --field=Username)
  rbw get "$login" | wl-copy
  notify-send "Passwords" "$login\n$user\nPassword copied to clipboard"
fi
